import React from 'react';

const HomePage = () => (
  <div>
    <h1>Le Documentaire</h1>
    <p>
      This is where all the public documents will be displayed,
      when the app is run for the first time.
      And it works as it should.
    </p>
  </div>
  );

export default HomePage;
